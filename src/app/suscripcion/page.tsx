"use client";

/**
 * Ruta pública /suscripcion (retornos de MercadoPago, enlaces externos, SEO).
 * El contenido vive en components/subscription para reutilizarlo como overlay.
 */
export { default } from "@/components/subscription/subscription-content";
